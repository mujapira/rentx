import { Inter, Archivo } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], variable: "--inter-ff" });
export const archivo = Archivo({ subsets: ["latin"], variable: "--archive-ff" });